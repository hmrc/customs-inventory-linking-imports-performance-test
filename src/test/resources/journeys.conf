journeys {

  submit-Validate-movement {
    description = "Send validate movement request"
    load = 50
    feeder = data/happy-path-local.csv
    parts = [
      submit-validate-movement
    ]
    //skip-if = [init]
  }

  submit-Goods-arrival {
    description = "Send goods arrival request"
    load = 50
    feeder = data/happy-path-local.csv
    parts = [
      submit-goods-arrival
    ]
    //skip-if = [init]
  }


  initialise-test-data = {
    description = "Create subcription fields"
    load = 1
    parts = [
      create-subscription-fields
    ]
    //run-if = [init]
  }


}
