
# This file is read when runLocal = false in application.conf (default is false)

  services {

    auth {
      protocol = https
      host = customs-api-hods-stubs.protected.mdtp
      port = 80
    }

    inventory-linking-imports {
      protocol = http
      host = customs-inventory-linking-imports.protected.mdtp
      port = 80
    }

  }
